pre-commit:
  parallel: true

  commands:
    check:
      glob: "*.{ts,tsx,js,jsx}"
      run: deno check
    fmt:
      run: deno fmt --check {staged_files}
    lint:
      glob: "*.{ts,tsx,js,jsx}"
      run: deno lint {staged_files}
